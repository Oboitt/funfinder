 <%= simple_form_for [activity] do |f| %>
    <%= f.input :date_begin,
              as: :string,
              input_html: { data: { controller: "datepicker" } } %>
    <%= f.submit class:"btn btn-ghost" %>
  <% end %>
